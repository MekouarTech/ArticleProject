@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "نسيت كلمة السر؟";
}

<h1 style="font-family: Cairo;direction:rtl">@ViewData["Title"]</h1>
<h2 style="font-family: Cairo;direction:rtl">أدخل بريدك الإلكتروني.</h2>
<hr />
<div class="row" style="font-family: Cairo;direction:rtl">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                <label asp-for="Input.Email" class="form-label"></label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">إعادة تعيين كلمة المرور</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
